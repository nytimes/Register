<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:baselineAligned="false"
    android:gravity="center_vertical"
    android:minHeight="40dp"
    android:clipToPadding="false"
    android:orientation="horizontal"
    android:paddingBottom="4dp"
    android:paddingStart="4dp"
    android:paddingEnd="0dp"
    android:paddingTop="4dp">

    <TextView
        android:id="@+id/nyt_register_item_config_code"
        android:layout_width="22dp"
        android:layout_height="wrap_content"
        android:layout_marginEnd="3dp"
        android:fontFamily="monospace"
        android:textColor="@color/nyt_register_config_gray"
        android:textSize="14sp"
        android:gravity="center"
        android:textStyle="bold"
        tools:text="-1" />

    <TextView
        android:id="@+id/nyt_register_item_config_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginEnd="6dp"
        android:layout_weight="1"
        android:ellipsize="marquee"
        android:singleLine="true"
        android:fontFamily="monospace"
        android:textColor="@color/nyt_register_config_gray"
        android:textSize="12sp"
        android:textStyle="bold"
        tools:text="RESULT_OK" />

    <ImageView
        android:id="@+id/nyt_register_item_config_down_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:scaleType="center"
        app:srcCompat="@drawable/nyt_register_ic_menu_down" />

</LinearLayout>